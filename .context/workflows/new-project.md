# New Project Workflow

This is a compatibility pointer for older instructions. Use `create-project.md` as the canonical workflow.

## Canonical Workflow
Follow `.context/workflows/create-project.md`.
