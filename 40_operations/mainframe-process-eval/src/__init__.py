"""Portable MainFrame Process Evaluation engine.

Owned by the `mainframe-process-eval` operation. Depends on MainFrame
lifecycle substrate (`lifecycle_identity`, `lifecycle_runtime`,
`migration_lease`). Must not be imported by that substrate.
"""
